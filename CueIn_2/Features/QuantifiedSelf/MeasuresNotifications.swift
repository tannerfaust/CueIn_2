import Foundation

extension Notification.Name {
    /// Opens the “add tracker” flow on the Measures tab (from the global + button).
    static let cueInShowAddMeasureTracker = Notification.Name("cuein.measures.showAddTracker")
}
